name: Update Finance News

on:
  schedule:
    # 每天 UTC 0:00（北京时间 8:00）
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Fetch finance news
        env:
          TZ: Asia/Shanghai
        run: node .github/scripts/update-news.js --topic=finance

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/finance.json data/finance/ feed-finance.xml .github/scripts/translations-cache.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "📈 金融资讯更新 $(date +%Y-%m-%d)"
            git push
          fi
